type numStr = number | string;
type condescrip = 'hi' | 'bye'
function combine(n1: numStr ,n2: numStr){
    let result;
    return result;
}

const combinedAges = combine(20,30);
console.log(combinedAges);

const combinedNames = combine(20,30);
console.log(combinedNames);